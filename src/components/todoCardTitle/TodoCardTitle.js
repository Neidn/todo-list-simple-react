import React from "react";

const TodoCardTitle = ({ title }) => {
    return (
        <h2 className="text-white dark:text-white text-lg font-medium">{title}</h2>
    );
}

export default TodoCardTitle;
